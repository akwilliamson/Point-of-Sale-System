require 'spec_helper'

describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end

describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end
describe Bundle do
  it 'updates the transaction total to the new total' do
    bundle = Bundle.create(:cashier_id => 1, :transaction_total => 3.05)
    bundle.add_ringup(10, 1.50)
    bundle.add_ringup(5, 2.00)
    bundle.transaction_total.should eq 28.05
  end
end

